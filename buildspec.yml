version: 0.2

phases:
  install:
    runtime-versions:
      python: latest
    commands:
      - echo Installing dependencies
      - apt-get update -y
      - apt-get install -y cowsay
  build:
    commands:
      - python --version | cowsay
